server {
    listen 80;
    server_name umitunker.com www.umitunker.com;

    # Redirect HTTP to HTTPS (uncomment after SSL is set up)
    # return 301 https://$host$request_uri;

    # Frontend static files at /uuai
    location /uuai {
        alias /var/www/umitunker/uuai;
        try_files $uri $uri/ /uuai/index.html;
        index index.html;
    }

    # Backend API proxy
    location /api {
        proxy_pass http://127.0.0.1:8001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 120s;
    }
}

# HTTPS block (uncomment after running: sudo certbot --nginx -d umitunker.com -d www.umitunker.com)
# server {
#     listen 443 ssl;
#     server_name umitunker.com www.umitunker.com;
#
#     ssl_certificate /etc/letsencrypt/live/umitunker.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/umitunker.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#
#     location /uuai {
#         alias /var/www/umitunker/uuai;
#         try_files $uri $uri/ /uuai/index.html;
#         index index.html;
#     }
#
#     location /api {
#         proxy_pass http://127.0.0.1:8001;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
